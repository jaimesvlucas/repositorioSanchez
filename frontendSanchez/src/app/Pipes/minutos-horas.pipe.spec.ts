import { MinutosHorasPipe } from './minutos-horas.pipe';

describe('MinutosHorasPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutosHorasPipe();
    expect(pipe).toBeTruthy();
  });
});
